# macOS
.DS_Store
**/.DS_Store

# Claude Code 로컬 설정 (배포 제외)
.claude/
**/.claude/

# 참고자료 폴더 (스킬 아님)
write-prd/
write-techspec/
tasks/

# 이진 파일 (PDF, Pages)
**/*.pdf
**/*.pages

# node
node_modules/

# CI/CD worktree 캐시
**/.claude/worktrees/

# 로컬 설치 스크립트 (npm 패키지에서는 bin/install.js가 담당)
install.sh
